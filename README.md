to-dos

1. replace in-memory rate limiting with redis replacing the currently used map which is essentially worthless in serverless functions

2. fix the issue of react.submitEvent and react.formEvent. 

